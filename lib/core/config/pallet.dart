import 'package:flutter/material.dart';

class Palette {
  Palette._();
  static const Color primary =
      Color(0xff0056D2); // A deeper blue for better contrast
  static const Color secondary = Color(0xff1E88E5); // Vibrant secondary blue
  static const Color primaryText = Color(0xff212121);
  static const Color secondaryText = Color(0xff616161);
  static const Color lightBackground = Color(0xffF3F4F6); // Softer background
  static const Color appBarBackground =
      Color(0xffF5F8FB); // Light grey-blue tone
  static const Color cardBackground = Color(0xffffffff);
  static const Color bottomNavBackground =
      Color(0xffEAF3F9); // Subtle tint for navbar
  static const Color divider = Color(0xffD9E2EC);
  static const Color shadow =
      Color(0xffA0AABF); // Lighter shadow for soft elevation
  static const Color orange = Color(0xffFF7043); // Muted orange for accents
  static const Color white = Color(0xffffffff);
}
